pipeline {
       agent any
             stages{
                  stage('maven version'){
                     steps{
                        script{
                          sh "mvn --version"
                          }
                         }
                   }
}
}
